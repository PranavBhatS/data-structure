/** 
1. WHat is the potential bug that you can find when using index as key
2. Lifecycle methods of class component
3. Context api
4. what is prop drillins and how can you overcome that using context api
5. useState, useEffect, useContext
6. Optiomization 
    Pure component
    memo
    useMemo and useCallback
7. How to share login across multiple component?
   Higher order component
   Render props pattern
   Custom hooks

8. What is the purpose of using super constructor with props argument?

Redux
1. What is redux?
2. How do you decide whether to choose context APi or redux?
3. Redux actions, actions creators,reducers
4. WHat exactly does the connect function do from the react-redux library?
5. what do mapStateToProps and mapDispatchToProps  actually do
6. why should you dispath an action to update the state and not update store directly?
7. In a reducer, why should you return a new object as state and not modify the existing state object?

*/


/*
setTimeout(function timeout() {
  console.log('Timed out!');
}, 0);
Promise.resolve(1).then(function resolve() {
  console.log('Resolved!');
});



function multiply(a, b) {
  return a * b;
}
console.log(multiply.bind(this, 2));


const ar = [1, 2, 3, 4];
for (var i = 0; i <= ar.length; i++) {
    setTimeout(() => {
      console.log(`current index is ${i}`);
    }, 2000);
}
*/